// console.log("suhail");



//The advantage of using the module is that suppose we have two variables of the same name and if we define those variables in separate modules they do not overwrite each other so basically we first call the module and then we specify which variable we are accessing so in this way the variables does not overlap here.

