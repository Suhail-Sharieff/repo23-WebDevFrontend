//DOM: document objext model
//BOM:Box objext model
//it is used to map html odcument


//dollar zeroo is the one referring to the current element


//for example if u want to generate an random number between a and b then type "Math.ceil(0+Math.random()*(b-a))"
//where the random number geneartd is between 0and 1

